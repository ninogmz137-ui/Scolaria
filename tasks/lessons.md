# Lessons Learned — Scolaria

<!-- Format : [date] | ce qui a mal tourné | règle pour l'éviter -->

[2026-03-29] | Provider nesting order in App.tsx caused APK crash (ActiveChildProvider used useAuth but was above AuthProvider) | Always check context dependency order when adding useX() hooks to a provider
[2026-03-29] | EXPO_PUBLIC_ env vars not in eas.json → missing at build time → runtime errors | Every EXPO_PUBLIC_ variable must be in BOTH .env (local dev) AND eas.json env (EAS builds)
[2026-03-29] | Hardcoded TOPBAR_HEIGHT per platform ignored safe area insets → overlap on Android | Always use useSafeAreaInsets() instead of hardcoded platform-specific padding
[2026-03-29] | API key 401 in production showed cryptic error | Always include actionable next step in user-facing error messages (where to regenerate, what to update)
[2026-03-30] | process.env.EXPO_PUBLIC_* undefined in EAS builds because .env is gitignored | NEVER rely on .env for EAS builds. Use app.config.js to read env vars at build time and inject into Constants.expoConfig.extra. Create a single getEnv.ts helper.
[2026-03-30] | EAS Secrets marked "Sensitive" don't get inlined into JS bundle | EXPO_PUBLIC_ vars must be "Plain text" visibility in EAS, or better: use app.config.js extra pattern
[2026-03-30] | Spent 4 builds debugging env vars without understanding Expo's env injection | Before fixing env issues: understand the FULL chain (where var is defined → how it reaches Metro → how Metro inlines it). Don't just add more env sources — verify the pipeline.
[2026-04-02] | Android elevation > 0 draws grey outline even with borderWidth: 0 — took 4 builds to find | On Android, ALWAYS set elevation: 0 on card components. Use only background color contrast for visual separation. Elevation is only for floating UI (modals, tab bars).
[2026-04-02] | Fixed GlassCard but missed 15+ other components with hardcoded borderWidth/elevation | Before claiming a visual fix is done: grep the ENTIRE codebase for the property. Fix once, fix everywhere.
[2026-04-09] | UI changes not visible despite correct file edits — multiple Metro servers running (8081, 8082, 8083) serving stale bundles | Always kill ALL node processes before starting a new server. User was hitting an old server with the old bundle.
[2026-04-09] | AriaAvatar change had no effect — MessagerieScreen had its own inline gradient avatars for Aria type, completely independent of AriaAvatar.tsx | Before editing a component, grep files_with_matches for the visual pattern (gradient colors, icon style) to find ALL rendering sites — not just the obvious component.
[2026-04-09] | White icon on white background — forgot to add shadow/border when switching from gradient to white background | When replacing a colored background with white/glass, ALWAYS add a shadow + slightly colored border to ensure visibility against light backgrounds.
[2026-04-10] | Safe area padding buried in contentContainerStyle caused flicker on first render before insets were computed | Own the safe area at the topmost container (paddingTop: insets.top on root View). Children don't need to know about it.
[2026-04-10] | KeyboardAvoidingView behavior=undefined on Android means keyboard hides the input bar | Always use behavior='height' on Android (behavior='padding' on iOS). Never leave behavior=undefined.
[2026-04-10] | gap: 8 on absolute-positioned flex container caused icon overlap on some Android builds | Replace gap with explicit marginRight/marginLeft on the affected children. gap in absolute containers is unreliable on Android.
[2026-04-17] | 7 EAS builds wasted because documented Android lessons (elevation nesting, undefined KAV behavior, low glass opacity) were only applied to some files — not ALL call sites | When a lesson logs a documented Android pattern, `grep files_with_matches` for the pattern across `src/**` and fix every occurrence in one pass. Never claim "Android polish is done" without the codebase-wide sweep.
[2026-04-17] | Bottom-sheet modal with `height: '88%'` + `marginBottom` overflowed the top safe area (ReglagesScreen) | For bottom-sheet modals inside a safe-area-padded overlay, use `flex: 1` on the sheet so the parent's padding + the sheet's marginBottom naturally constrain its size. Percentage heights ignore siblings' margins.
[2026-04-17] | Splash `backgroundColor: '#FAFAF8'` didn't match the icon PNG's white background → visible seam / "ghost" on Android splash | Match splash `backgroundColor` exactly to the splash image's actual background color. For a white-bg icon, use '#FFFFFF'.
[2026-04-17] | Static `paddingBottom: getInputBarPaddingBottom(insets.bottom)` on composer UniversalInputBar reserved 72+inset px for the floating tab bar even when the keyboard was open on Android → input masked or pushed offscreen under `behavior='height'` KAV | When a composer sits above a floating tab bar, make its bottom padding dynamic via `Keyboard.addListener('keyboardDidShow'/'keyboardDidHide')`. Shrink to ~12px gap while open, restore full tab bar clearance on hide. Centralize in a hook (`useKeyboardInputPadding`) to avoid per-screen drift. [obsolète depuis 2026-09-19 : FloatingTabBar supprimé]
[2026-04-17] | Bottom sheet drag-to-close needed to stay compatible with existing reanimated `useSharedValue` translateY | Use `PanResponder` from RN core (no new lib) in a `useMemo`, write `translateY.value = Math.max(0, g.dy)` during `onPanResponderMove`, close if `g.dy > 100 || g.vy > 0.8` on release, otherwise `withTiming(0)`. Attach `{...panResponder.panHandlers}` to a dedicated `handleWrap` View above the sheet header so inner buttons remain tappable.
[2026-04-17] | Silent child-ID mismatch: `ActiveChildContext` used `demo-lea/demo-lucas/demo-emma` while `childContext.ts MOCK_CHILDREN` used `'1'/'2'/'3'` → `getChildContext(childId).find()` always fell through to `MOCK_CHILDREN[0]` (Léa), so Aria discussed Léa even when user selected Emma or Lucas. No runtime error. | When two data files describe the same entities (children, classes, etc.), the ID scheme MUST be identical across all of them. After adding a new entity source, `grep` the full set of IDs across `src/` to verify. `.find(...) ?? fallback[0]` patterns hide this bug — add a `console.warn` in the fallback branch during dev so the mismatch is visible. Defaults on function params (`childId = '1'`) must also match the canonical scheme.
[2026-04-18] | Android topbar round icon buttons (40×40, solid white bg) had no shadow because Agent A chose a hairline border over `elevation` (conservative memory of the 2026-04-02 grey-frame lesson). Buttons looked flat against the gradient header. | The 2026-04-02 rule (elevation draws a grey frame on Android) applies to *card* surfaces — wide, low-contrast, no `borderRadius > 20`. For small circular/pill floating buttons with a solid opaque background and no `borderWidth`, `elevation: 4` is safe and renders a clean native shadow. Keep `borderWidth: 0` and high-opacity background; skip any hairline border.
[2026-04-18] | Aria suggestion chips used `flexWrap: 'wrap'` + `width: '48%'` — the layout silently collapsed to a single row of crushed chips on Android Yoga, despite rendering correctly on web. | Never use `flexWrap: 'wrap'` + percentage child widths for horizontal chip lists on Android. Prefer a `<ScrollView horizontal showsHorizontalScrollIndicator={false}>` with explicit `minWidth` / `maxWidth` / `minHeight` per chip and `marginRight` spacing. The ScrollView guarantees chips stay at intrinsic size and scroll off-screen rather than compressing.
[2026-04-18] | In-drawer ScrollView set `paddingBottom: TAB_BAR_SCROLL_PADDING` (120px) — same value used by main screens to clear the floating tab bar. Inside a drawer the floating tab bar is not present, so the 120px padding pushed the last conversation items under the drawer's own search bar. | `TAB_BAR_SCROLL_PADDING` is only for ScrollViews behind the floating tab bar at root. Inside modals, drawers, sheets, or any surface that covers the tab bar, use a small intrinsic bottom padding (16–24px). Centralize the distinction: if the container is above the tab bar, no need to clear it. [obsolète depuis 2026-09-19 : FloatingTabBar supprimé]
[2026-04-18] | `gap: 4` inside a filter pill (Pressable with icon + label + chevron) rendered all three on separate lines on Android. | `gap` on `flexDirection: 'row'` has intermittent support on older Android / Yoga paths. Always replace with explicit `marginRight` on all but the last child. This extends the 2026-04-10 lesson from absolute containers to any row where child order matters visually.
[2026-05-11] | When removing a FAB, also delete its constants (FAB_SIZE, FAB_GUTTER, FAB_GAP_ABOVE_TAB_ROW, fabRowBottom), styles (fabSlot, fabPress, fabInner), and imports (MessageCircle, Plus) in the same pass. Leaving dead code causes TS warnings and confuses future readers. | Whenever a UI element is removed, grep the file for all names introduced by that element and delete them in the same commit.
[2026-05-11] | A button appearing in two separate JSX branches (two scroll views / two render paths) is easily missed when only the first instance is found via grep. | After finding the first occurrence of a removed element, run grep again for all remaining hits in the same file before closing the task.
[2026-04-18] | FAB rendered `<LinearGradient style={styles.fabGradient}>` but the style was never defined → `undefined` style prop → LinearGradient collapsed to zero size with no visual error at build time. | When a LinearGradient fills a flex parent (FAB inner, card header), it MUST have explicit `width` + `height` (or `width: '100%'`, `height: '100%'`) defined. Define a sibling `fabGradient` / `gradientInner` style at the same time as the FAB style — pair them in one commit, never split across agents. TypeScript won't catch the missing style because `styles.x` returns `undefined` at runtime.
[2026-04-18] | FloatingTabBar rendered on top of a modal screen (Réglages) and on top of in-screen sidebar drawer (Aria) — `<Modal>` and in-Stack `transparentModal` both render BELOW the tab bar because the tab bar is owned by the parent Tab.Navigator. | For screens that must cover the floating tab bar (full-screen modals, sidebars), add a route-based opt-out in `FloatingTabBar.tsx`: `const ROUTES_HIDE_TAB_BAR = new Set([...]); if (ROUTES_HIDE_TAB_BAR.has(nestedRouteName)) return null;`. Don't try to cover the tab bar structurally — you'll lose every time to the navigator's z-order. [obsolète depuis 2026-09-19 : FloatingTabBar supprimé]
[2026-04-18] | `ChildPopover` in `FloatingTabBar.tsx` wrote three `sharedValue.value = withTiming(...)` **directly in the component render body** (pattern: `if (visible) { opacity.value = ...; ... } else { ... }`). Under the New Architecture (Fabric), this throws `[Reanimated] Writing to value during component render` on every render. Because `FloatingTabBar` re-renders on every tab press and route change, the warning multiplied to 60+ per navigation — and produced stale-frame rendering artifacts (ghost blocks, misplaced overlays on sibling screens). | Never mutate `sharedValue.value` in a component's body. The only safe places to write to a sharedValue are: (1) inside `useEffect` keyed on the relevant state, (2) inside event handlers (`onPress`, `PanResponder.onPanResponderMove`, etc.), (3) inside `runOnUI` callbacks. Pattern fix: wrap the whole `if (visible) { ... } else { ... }` block in `useEffect(() => { ... }, [visible, ...sharedValues])`. Under Fabric / strict mode this is not just a warning — it's a correctness bug that can cascade. [obsolète depuis 2026-09-19 : FloatingTabBar supprimé]

[2026-05-13] | For screen-level actions (open modal, trigger form) wired to the BottomBar, the cleanest pattern in this codebase is a module-level ref (agendaActionRef, actionNavRef) set inside TabContentWithNav (which has access to useNavigation). Never pass callbacks through many levels of props — wire once at the navigator level via a ref. | When adding a new contextual button action, add a moduleRef + set it in TabContentWithNav + call it in the onActionPress switch in TabNavigator.
[2026-05-13] | For per-tab action overrides in BottomBar, implement a local handleActionPress switch INSIDE the component rather than relying on the parent to pass the right callback. This keeps navigation logic co-located with the component that owns the tab context and avoids prop-drilling. | When a bottom bar button needs different behaviour per active tab, use an internal switch(activeTab) handler; only fall through to onActionPress for generic/parent-controlled cases.
[2026-05-13] | overflow:hidden on a View/Pressable with elevation on Android kills the shadow — the clip and the shadow must live on separate Views. Pattern: outer View (shadow + borderRadius, no overflow) wrapping inner Pressable/View (overflow:hidden + borderRadius). | Whenever a card needs both clip (overflow:hidden) and elevation/shadow, always use the two-layer pattern: rowShadowWrap + row (or equivalent).
[2026-05-13] | measureInWindow on Android returns absolute screen coords. Don't re-derive dropdown position from windowW/insets in a useMemo — the measureInWindow callback already has the right x/y. Compute width/left directly in the callback (pillRight - dropW) and pass through verbatim. The useMemo then just becomes a null-guard passthrough. | For any positioned dropdown/tooltip anchored to a pill, do ALL layout math in the measureInWindow callback; useMemo should only gate on null.
[2026-05-13] | Placing a PanResponder wrapper on a FlatList to block the tab navigator swipe blocks ALL horizontal gestures including the FlatList's own pagingEnabled scroll — the FlatList never gets its touches. The right level for a strip PanResponder is on the static header element (weekStrip View), not on the scrollable body. Use onMoveShouldSetPanResponderCapture:true on the strip so it wins before the tab navigator, and leave the FlatList untouched. | Never wrap a horizontal pagingEnabled FlatList in a PanResponder View. Attach gesture capture to the static strip instead.

[2026-05-16] | Pour un hero gradient visible sous le ScrollView, mettre le LinearGradient en position:'absolute' dans le root View (avant le ScrollView dans le JSX), et ajouter backgroundColor:'transparent' sur le ScrollView. Le root View ne doit pas avoir overflow:'hidden'. | Hero gradient : LinearGradient absolute dans root View + ScrollView transparent. Jamais overflow:'hidden' sur le parent du gradient absolu (bug shadow clipping Android).
[2026-05-16] | Pour un avatar bouton avec gradient et borderWidth sur Android : outer View (shadow/elevation, NO overflow) → inner View (overflow:'hidden', borderWidth, borderRadius) → LinearGradient absoluteFill. Mettre elevation sur l'outer View uniquement — jamais sur une View avec overflow:'hidden'. | Avatar gradient button = pattern deux couches : outer (shadow) + inner (clip + border).
[2026-05-16] | Le wallpaper AccueilScreen (WallpaperContext, useWallpaper) est le fichier qui gère le système de fonds. Chemin : src/contexts/WallpaperContext.tsx + src/screens/WallpaperPickerScreen.tsx. Le hero gradient remplace désormais le wallpaper sur Accueil — rebrancher WallpaperContext en Phase 2 si nécessaire. | Ne pas supprimer WallpaperContext — le laisser intact pour les autres écrans et pour le branchement futur.
[2026-05-16] | TopBar pills inactives sur fond uni (Notes/Agenda/Messages) étaient transparentes → icônes illisibles. Pills actives en rgba(blanc,0.42) illisibles hors gradient. | Toujours différencier les styles pill selon isHome (fond gradient vs fond uni) : pillActive et pillInactive split en *Home/*Other. isHome = activeTab === 'accueil'.
[2026-05-16] | Pressable sur Android peut provoquer un layout cassé sur les rows avec flex:1 sur un enfant texte. Switcher vers TouchableOpacity règle le problème systématiquement pour les listes de sélection. | Pour les sheets de sélection (ChildSelectorSheet, etc.) : toujours TouchableOpacity + activeOpacity=0.75, jamais Pressable.
[2026-05-16] | NotesScreen avait un C local avec violet:'#7C3AED' utilisé dans les pills, SVG, et 30+ styles. Pour purger une couleur interdite : d'abord mettre à jour la valeur dans l'objet palette local (C.violet='#4338CA'), puis grep les rgba() hardcodés de la même couleur (rgba(124,58,237,...)) et les remplacer aussi. tsc --noEmit confirme qu'aucune utilisation ne reste. | Quand on purge une couleur dans un écran qui a une palette locale, utiliser replace_all sur chaque variante rgba() en plus de mettre à jour la palette.
[2026-05-17] | Les couleurs dans TAG_STYLES (catégories Devoir/Contrôle) et les avatarColor dans messagerieData pour les identités de personnes sont des couleurs UI, pas des données scolaires — elles sont exemptées de la règle "jamais de vert/rose sur données". Seules URGENCY_STYLES (badges d'action urgente) et les tendances/stats doivent suivre la palette stricte. | Distinguer couleurs UI d'identité (tags catégorie, avatars personnes) vs couleurs données scolaires (notes, tendances, moyennes). La purge ne cible que ces dernières.
[2026-05-17] | FAB dans un ScrollView avec Modal : placer le FAB AVANT le Modal dans le JSX (sinon le Modal l'écrase). Utiliser un pointerEvents="box-none" wrapping View pour que les events scroll passent à travers. Style outer = shadow, style inner = Pressable (pas overflow:hidden sur la View shadow). | FAB dans écran avec Modal : JSX order = FAB avant Modal, pattern shadow-outer + Pressable-inner, pointerEvents box-none sur le wrapper absolu.

[2026-05-17] | `gap` dans un participant strip (flexDirection:'row') sur Android provoque un spacing cassé — même constat que la règle globale `gap` sur row. | Remplacer tout `gap` dans les strips/rows par `marginRight` sur le premier enfant. S'applique aussi à `dateSep` avec lignes latérales.

[2026-05-17] | Les styles dead code (InfoRow/InfoRowProps après suppression du composant) ne causent pas d'erreur TypeScript mais accumulent de la dette. | Quand un composant sub est supprimé du JSX, supprimer immédiatement son interface, sa définition et ses styles associés dans la même passe.

[2026-09-18] | Le web (react-native-web) utilise le moteur CSS du navigateur, PAS Yoga. Un écran OK sur le web ne prouve rien pour Android. | Toute vérification visuelle se fait sur le dev client Android (`npm run dev:android`). Build EAS preview uniquement en fin de sprint.
[2026-09-18] | Un export web (`expo export`) embarque le bundle JS complet, donc toute clé EXPO_PUBLIC_* dedans. | Ne jamais commiter un export web. Aucune clé dans le bundle : tout secret vit côté serveur, dans une Edge Function Supabase.
[2026-09-18] | Archive EAS de 532 Mo faute de .easignore complet. | Toujours un .easignore qui exclut node_modules, les dossiers de build natifs et .git.
[2026-09-18] | Le dossier android/ généré par prebuild était absent de .easignore : EAS l'uploadait (1,9 Go) et détectait à tort un projet bare, d'où l'échec sur runtimeVersion et une config native figée qui pouvait écraser app.config.js. | Un projet managé n'uploade JAMAIS android/ ni ios/ vers EAS. Ces dossiers sont dans .gitignore, ils doivent aussi être dans .easignore.
[2026-09-18] | Le forçage du mode sombre de MIUI inversait toutes les couleurs de l'app, rendant le wordmark navy illisible et faussant tous les tests visuels. | Une app sans mode sombre DOIT déclarer userInterfaceStyle: 'light' et android:forceDarkAllowed=false. Sinon Android/MIUI inverse les couleurs de force.
[2026-09-18] | userInterfaceStyle: 'light' était déjà dans app.config.js mais sans effet sur Android, faute du package expo-system-ui. Le réglage était mort depuis le début. | Sur Android, userInterfaceStyle nécessite expo-system-ui. Un réglage présent dans la config ne prouve pas qu'il est actif : vérifier le package requis.
[2026-09-19] | 39 fichiers morts (7500 lignes) et FloatingTabBar démonté mais dont les constantes dimensionnaient encore 55 lignes de padding. Un toggle corrigé la veille se trouvait dans un écran mort. | Supprimer le code mort AVANT tout audit ou correction : on débogue sinon des écrans jamais affichés.

[2026-09-22] | sed -i (Git Bash) a converti TabNavigator.tsx de CRLF en LF : faux diff de 1001 lignes. Un script node qui cherchait "
" dans un fichier CRLF a dupliqué SchoolModeContext. | Le dépôt mélange CRLF et LF (autocrlf=true). Jamais de sed -i sur src/. Éditer avec Edit ou un script qui normalise en 
 puis restaure l’EOL d’origine, et contrôler git diff --numstat après chaque passe.
[2026-09-22] | Pas de police par défaut : tout Text sans fontFamily sortait en police système, et Text.defaultProps ne marche plus (React 19). | Un seul point d’entrée : le Text de src/components/ui (Figtree, graisse → variante, fontWeight retiré, cssInterop pour className). Ne jamais importer Text de react-native.
[2026-09-22] | Les anciens fonds survivaient dans les tokens eux-mêmes (SCREEN_BACKGROUND et C.bg à #F7F7F5) et dans le thème par défaut de React Navigation (rgb(242,242,242)). | Pour une couleur globale : corriger d’abord le token source, puis le thème NavigationContainer, puis chercher aussi les classes NativeWind (bg-…) et les formes rgba(), pas seulement les hex.
[2026-09-22] | Remplacer un LinearGradient par une View dans un fichier qui n’importe que Box (ui) a cassé la compilation, et laissé des imports LinearGradient inutiles. | Après une conversion JSX automatique : tsc, puis grep des imports devenus inutiles dans les fichiers touchés.
[2026-09-22] | DETTE TECHNIQUE : l’export PDF (services/pdfExport.ts) utilise -apple-system / Segoe UI, pas Figtree (police non embarquée dans le HTML imprimé). | Au sprint export : embarquer Figtree en @font-face base64 dans le HTML du PDF.
[2026-09-23] | Wordmark en sans-serif sur Android : SvgText utilisait fontFamily="Rufina-Bold" alors que expo-font charge la police sous le nom Rufina_700Bold. Le web masquait le problème (repli silencieux). | Toujours utiliser le nom de clé exact passé à useFonts (Rufina_700Bold, Figtree_700Bold…). Jamais de fontWeight sur une police custom : sur Android il fait retomber en police système.
[2026-09-23] | ScolariaSymbol en « tirets » sur Android : <G rotation originX> imbriqués mal appliqués par react-native-svg Android. | En SVG natif, une seule transformation par forme, en transform="rotate(angle)" (rotation autour de l’origine du viewBox). Pas de rotation/originX sur des <G> imbriqués.
[2026-09-23] | Cartes Agenda sans barre ni fond sur Android : style en fonction ({ pressed }) sur Pressable + fond color + "12" (hex 8 chiffres concaténé) + barre en View enfant sans hauteur. | Pour une carte colorée : style statique sur TouchableOpacity, borderLeftWidth + borderLeftColor (pas de View-barre), fond calculé en rgba() par un helper (withAlpha), jamais color + "12". [hypothèse de cause à confirmer sur appareil]
[2026-09-23] | Contenu sous la barre du bas : le voile flou (inset + 106 px) était plus haut que le padding de défilement (inset + 90 px). | Une seule mesure pour toute la zone du bas : getBottomChromeHeight() sert au voile ET à getBottomBarScrollPadding(). Tout élément ajouté sous un champ qui porte la réserve du bas se retrouve sous la barre : placer les mentions AU-DESSUS du champ.
[2026-09-23] | Bouton rond « Tools » vu sur tous les écrans : c’est le FAB d’expo-dev-menu (code dans android/src/debug, remplacé par disableInRelease en release). | Ne pas le masquer dans le code : il n’existe que dans le dev client. Pour les captures, le désactiver dans le menu dev (section Tools).
[2026-09-23] | TextInput restait en police système malgré le Text Figtree global. | Text ET TextInput viennent de src/components/ui (wrapper Figtree + cssInterop). Ne jamais importer Text ni TextInput de react-native.
[2026-09-23] | « Remplace le composant par ScolariaSymbol » : c’était déjà ScolariaSymbol. Les tirets venaient de la géométrie à petite taille (ellipses de 1,4 px de large à 14 px), pas du rendu. | Avant de corriger un visuel, grep le composant réellement rendu ET calcule la taille réelle des formes en px. Un symbole détaillé a besoin d’une géométrie compacte sous ~32 px.
[2026-09-23] | AriaOrb recopiait la couronne (avec les <G rotation> bogués) : la correction de ScolariaSymbol ne l’atteignait pas. | Une forme de marque = une seule définition exportée (CrownShapes). Grep la géométrie (rx, ry, cy, angles) pour trouver les copies.
[2026-09-23] | Deuxième écran cassé par le même motif (Agenda en 0-bis, Mon compte en 0-ter) : Pressable de react-native + style en fonction ({ pressed }) → ignoré sur Android avec NativeWind (flexDirection, fond, bordures perdus). | Corriger le motif à la source, pas écran par écran : Pressable vient de src/components/ui, qui résout la fonction de style. Ne jamais importer Pressable, Text ni TextInput de react-native.
[2026-09-23] | Initiales « LM » pour Léa et Lucas Moreau : le nom de famille est commun à toute la fratrie. | Initiale du PRÉNOM seulement (getChildInitials), deux lettres si deux prénoms commencent pareil.
[2026-09-23] | Top bar passée en bandeau opaque #F2F1EE en 0-ter pour régler le contenu qui défilait dessous : contraire au design. | RÈGLE : la top bar (et la bottom bar) n’est JAMAIS un bandeau opaque. Au repos, transparente ; au défilement, un voile ScrollVeil (dégradé #F2F1EE opaque jusqu’au milieu de la barre puis fondu de 24 px), opacité liée à scrollY (0 → 1 sur 16 px). Un seul composant pour le haut et le bas.
[2026-09-23] | Voiles en BlurView (expo-blur) sous les barres : instables sur Android. | Pas de BlurView pour les voiles de navigation : expo-linear-gradient + opacité Reanimated.
[2026-09-23] | Le défilement remontait par un callback JS + withTiming à seuil (y > 12) : saut brusque, et un onglet revenu gardait le voile de l’onglet précédent. | Défilement = valeur partagée (useAnimatedScrollHandler sur Animated.ScrollView) interpolée côté UI ; chaque écran republie son propre dernier scroll au focus (useTopbarScrollHandler).
[2026-09-23] | Pills de top bar sombres illisibles sur le header indigo de l’Accueil une fois la barre redevenue transparente. | Tout élément posé sur un fond variable doit avoir deux états de contraste : pills claires sur le header (au repos), sombres dès que le voile #F2F1EE apparaît (scrollY > 8).
[2026-09-23] | En vérifiant .env, un sed de masquage a laissé passer une ligne de continuation : un fragment de la clé Anthropic s’est affiché dans le journal de session. | Ne JAMAIS afficher le contenu de .env, même partiellement ni « masqué » : uniquement grep -c, noms de variables via un script, ou longueur (value.length).
[2026-09-23] | Clés Anthropic et Google Vision en EXPO_PUBLIC_* → injectées dans extra (app.config.js) → lisibles en clair dans l’APK, et journaux de démarrage affichant les 12 premiers caractères. | Aucune clé d’API tierce côté app. Les appels passent par une Edge Function Supabase ; la clé est un secret Supabase (supabase secrets set). Côté app, EXPO_PUBLIC_* = valeurs publiques uniquement (URL + clé anon Supabase). Jamais de clé, même tronquée, dans un console.log.
[2026-09-23] | Les messages d’erreur d’Aria parlaient de clé API, de .env et d’eas.json à l’utilisateur final. | Un seul message utilisateur pour toute panne d’Aria : « Aria est momentanément indisponible. » Le détail technique reste dans les journaux serveur.
[2026-09-23] | Choix du modèle Aria fait sans l’utilisateur (Opus par défaut, repli automatique activé) : décisions de coût et de comportement qui lui reviennent. | Le modèle, son coût et tout comportement automatique (repli, relance) se font valider avant d’être codés ; toujours un secret (ARIA_MODEL) pour changer de modèle sans redéployer.
[2026-09-23] | Le protocole d’urgence n’existait nulle part, et le mode démo aurait répondu par un exemple à un message de détresse. | Toute règle de sécurité d’Aria vit dans un module partagé (supabase/functions/_shared) appliqué côté serveur (fait foi) ET côté app (démo). Tester les faux positifs de mots courts avec  (viol ≠ violon / violent).
[2026-09-23] | Le numéro 3020 (hors service depuis le 1er janvier 2024) était affiché dans l’app et repris tel quel de CLAUDE.md dans le protocole d’urgence. | Tout numéro d’aide doit être vérifié à la source officielle avant d’être codé, jamais recopié de la doc interne ; le test du protocole vérifie la présence des bons numéros et l’absence des anciens.
[2026-09-23] | `supabase db dump` et `functions deploy` ont besoin de Docker par défaut ; Docker Desktop était arrêté. | Déploiement : `functions deploy <nom> --use-api` (assemblage côté Supabase, sans Docker). Sauvegarde sans Docker : DDL reconstitué depuis les catalogues (pg_get_constraintdef, pg_indexes, pg_policies, pg_get_functiondef, pg_get_triggerdef, pg_views) + contrôle des décomptes contre la base (tables, FK, CHECK, index, policies…).
[2026-09-23] | Les fichiers supabase/*.sql locaux ne reflétaient plus la base : 12 tables manquantes, colonnes emoji supprimées en base mais encore lues par l’app. | La base réelle fait foi. Avant tout plan de migration : lire le schéma réel et le comparer. Ensuite, une seule source : supabase/migrations/ (une migration réversible par sujet).
[2026-09-23] | Advisors Supabase : 3 vues en SECURITY DEFINER exposant tous les enfants, rôle enseignant auto-attribuable ouvrant toutes les données, publications de classe lisibles par tous. | Toute vue exposée : `security_invoker = true`. Jamais d’autorisation fondée sur une colonne que l’utilisateur peut modifier lui-même (profiles.role). Lancer get_advisors (sécurité) après chaque migration.
[2026-09-23] | La CLI Supabase lit tout .sql de supabase/migrations comme une migration : un _down.sql au même numéro aurait créé un conflit de version ; et apply_migration (MCP) choisit sa propre version, antérieure ici à la baseline. | Scripts inverses dans supabase/migrations_down/. Après apply_migration : list_migrations, renommer le fichier local sur la version distante, puis `supabase migration list` doit montrer local = distant.
[2026-09-23] | `\?` avec Remove-Item (PowerShell 5.1) n’a pas trouvé le dossier « nul » (nom réservé Windows), alors que Git Bash le voyait. | Pour les noms réservés Windows (nul, con, aux…), passer par Git Bash (`rm -rf ./nul/`) et vérifier ensuite l’absence.
[2026-09-23] | Tester une policy/un trigger sans risque pour les données. | Transaction annulée : `begin; set local role authenticated; select set_config('request.jwt.claims', …, true); <requête>; rollback;` — refus attendu = erreur 42501.
[2026-09-23] | Secret ANTHROPIC_API_KEY collé sur deux lignes → Deno a levé « Invalid header value » en recopiant la clé complète, et `console.error('…', error)` l’a écrite dans les journaux de la fonction. | Dans une Edge Function : valider le secret (absent / espace / retour à la ligne) sans jamais le journaliser ; ne JAMAIS journaliser un objet d’erreur ou un error.message brut (ils peuvent contenir des en-têtes) — seulement statut, type, nom.
[2026-09-23] | Un compte réel sans enfant retombe sur les enfants de démo : Aria aurait reçu le contexte de Léa. | Tout appel à Aria vérifie l’origine du contexte : jamais de données de démo pour un compte réel (contexte neutre si aucun enfant).
[2026-09-23] | Un lien créé par trigger AFTER INSERT n’existe pas encore quand la RLS vérifie les lignes renvoyées par `insert(...).select()` : une policy fondée uniquement sur ce lien aurait fait échouer createChild. | Pour une table dont l’accès dépend d’un lien créé par trigger, la policy SELECT accepte aussi le créateur (`parent_id = auth.uid() OR is_responsable(id)`).
[2026-09-23] | Tester des règles d’accès multi-utilisateurs sans laisser de trace. | Bloc DO : créer des auth.users fictifs (@test.invalid), `SET LOCAL ROLE authenticated` + claims par utilisateur, compter ce que chacun voit, puis `RAISE EXCEPTION` avec le JSON des résultats : tout est annulé, le résultat arrive dans le message d’erreur. Vérifier ensuite qu’il ne reste aucune donnée fictive.
[2026-09-23] | Un filtre `.eq('parent_id', user.id)` dans l’app contournait la logique « responsables » : un second responsable n’aurait vu aucun enfant. | Quand la RLS définit le périmètre, l’app ne refiltre pas sur le créateur : elle lit tout ce que la RLS autorise.
[2026-09-24] | Lot 2 : le créateur gardait l’accès via parent_id et un trigger créait le lien après coup — contournement fragile. | Création d’une entité + de ses liens d’accès + de ses données obligatoires = UNE fonction transactionnelle (create_child), et l’insertion directe est retirée. Les invariants (« jamais d’enfant sans année ») = contrainte différée vérifiée au COMMIT.
[2026-09-24] | Rattachement à un enfant : personne ne doit pouvoir s’ajouter seul. | Aucun INSERT direct sur une table de droits ; ajout uniquement par invitation créée par un ayant-droit et acceptée par l’invité dont l’email de compte est CONFIRMÉ ; retrait limité à soi-même, jamais du dernier responsable.
[2026-09-24] | apply_migration (MCP) renumérote les migrations, ce qui obligeait à renommer les fichiers. | Préférer `supabase db push` (dry-run d’abord) : la version appliquée = le nom du fichier local, aucune recopie du SQL.
[2026-09-24] | M9 : trigger SECURITY DEFINER testant `current_user IN ('anon','authenticated')` — dans une fonction DEFINER, current_user = propriétaire (postgres), le test n’était jamais vrai (source jamais calculée ; vu par les tests, corrigé par M9b). | Un trigger qui doit connaître le rôle appelant est SECURITY INVOKER ; il délègue les lectures protégées à des fonctions d’aide DEFINER. Toujours tester l’écriture sous le rôle authenticated avant de conclure.
[2026-09-24] | Couleur de démo d’Emma en ambre, alors que l’ambre est réservé au Score de Joie (CLAUDE.md). | Avant de choisir une couleur, vérifier les couleurs réservées (ambre = Score de Joie, dégradé Aria, indigo = accent) et l’interdit vert/rouge sur les données.
[2026-09-24] | Contrôle des fins de ligne avec grep $'\r$' sous Git Bash : 0 partout, alors que TabNavigator.tsx était en CRLF ; le script de remplacement a échoué (0 correspondance). | Vérifier l’EOL avec node (`s.includes('\r\n')`) ; tout script d’édition normalise en \n, applique, puis restaure l’EOL d’origine ; contrôler git diff --numstat.
[2026-09-24] | Scripts JS passés en heredoc ou en node -e dans l’outil Bash : guillemets et \n mangés, commande bloquée (cat sans entrée). | Écrire les scripts d’édition dans le scratchpad (outil Write) puis `node script.js`.
[2026-09-24] | navigate('MainPager', { screen: 'Accueil', params: { screen: X } }) faisait de X la racine de la pile : pas de bouton retour, goBack impossible. | Pour empiler une page profonde depuis un autre endroit : `params: { screen: X, initial: false }`.
[2026-09-24] | Les écrans de réglages affichaient des options branchées sur rien : haptique jamais appelé, taille du texte jamais lue, « Face ID » en dur, préférences de notifications écrites dans une colonne inexistante. | Avant d’afficher un réglage, grep qui le LIT. Rien ne le lit → ne pas l’afficher, ou le signaler [UNCLEAR] s’il fait partie d’une structure validée.
[2026-09-24] | Réponses et checklist Redmi rédigées en anglais alors que l’utilisateur travaille en français. | Toujours répondre en français (réponses, checklists, messages d’avancement) ; seuls les identifiants de code restent tels quels.
[2026-09-24] | Pills de top bar blanches sur l’Accueil : la barre était posée sur un header indigo plein écran, d’où une 2e palette « sur header » contraire à COMPONENTS §0. | Une barre de navigation se pose toujours sur #F2F1EE : un header coloré est une carte SOUS la barre, jamais derrière. Une seule palette de pills.
[2026-09-24] | À propos affirmait « 100 % Europe », « AES-256 », « RGPD conforme », « hébergé en France », Google Vision… sans aucune vérification (Aria appelle Anthropic hors UE). | Aucune affirmation de sécurité, d’hébergement ou de conformité à l’écran sans preuve vérifiée ; en cas de doute, ne garder que les engagements tenus (0 pub, 0 revente).
[2026-09-24] | Écran Autorisations peuplé de rôles fictifs (grand-mère, médecin, 4 niveaux) sans lien avec la table responsables. | Un écran de droits d’accès reflète UNIQUEMENT les droits réels en base ; les idées futures vont au todo, pas à l’écran.
[2026-09-24] | npx supabase : cache npx cassé (paquet sans binaire), ENOENT. | Utiliser `npx supabase@latest` (cache complet) ; en cas d’ENOENT, supprimer le dossier _npx fautif.
[2026-09-24] | Données de démo servant de repli pour de VRAIS enfants : Aria (`getChildContext` par prénom puis Léa, envoyé à Anthropic), profil (`default:` = Emma), Notes (`MOCK_SUBJECTS`), maternelle (domaines de Léa pour tout enfant), parcours (années d’Emma). | Aucun repli sur la démo : un id inconnu renvoie null / [] et l’écran affiche un état vide. Grep `default:`, `?? MOCK`, `[0]` et les ids `demo-` à chaque écran touché.
[2026-09-24] | `scheduleConseilDuMatin()` programmait au login, pour tout compte, 7 push hebdomadaires sur « Lucas ». | Jamais de notification programmée avec du contenu fictif ; toute notification nomme l’enfant du compte et respecte les règles de CLAUDE.md (18h, silence 20h–7h).
[2026-09-24] | Tests web dans le panneau masqué : les animations (requestAnimationFrame) sont suspendues → sélection d’enfant retardée (elle attendait la fin d’un fondu) et Modal du sélecteur figée ouverte. | Ne jamais conditionner un état métier à la fin d’une animation (état d’abord, animation ensuite). En test web masqué, exclure les [aria-modal] du contrôle et vérifier par le DOM, pas par capture.
[2026-09-24] | Réécriture d’un JSON de démo par JSON.stringify : format changé, diff de +1116 lignes pour retirer 57 entrées. | Pour retirer des entrées d’un fichier de données, supprimer les lignes concernées et garder le format ; contrôler git diff --numstat (retraits seulement).
[2026-09-24] | Données de démo contradictoires entre fichiers (Lucas CE2 Jules Ferry vs CM2 Voltaire ; Emma 4e Jean Moulin vs 3e Collège Hugo ; Mme Dupont chez Lucas ET Emma). | Une seule référence de l’univers de démo (src/data/demo/suivi.ts, en-tête — carnet.ts supprimé le 25 sept) ; tout nouveau contenu démo s’y aligne ; test automatique « marqueurs des autres enfants » par écran.
[2026-09-24] | Header en fondu : l'ancien dégradé passait de la couleur à #F2F1EE (et un fondu vers 'transparent' = noir transparent sur Android) → zone grise terne ; le bloc arrondi faisait bandeau. | Fondu = même RGB, seule l'opacité varie (withAlpha), 10 arrêts ease-out sur ~300px. Tout texte posé sur un fond variable mesure sa position (onLayout) et prend le ton lisible ; contrastes calculés sur toutes les couleurs d'enfant avant de valider.
[2026-09-24] | useMemo de données de démo avec des dépendances vides : les devoirs d’Emma restaient affichés pour Léa (GS). | Toute donnée dérivée de l’enfant actif dépend de selectedChild (id / cycle) ; test croisé des 3 enfants de démo sur CHAQUE onglet et filtre, pas seulement la vue par défaut. (Correctif B2-ter : la règle « devoirs réservés au collège » appliquée ensuite était fausse — devoirs dès le CP.)
[2026-09-24] | Score de Joie affiché en chiffres (4.0/10, -29 %) avec alertes, dans 5 endroits (profil, curseur, PDF, mémo, réponses Aria). | Avant de valider un écran, grep « /10 », « % », « joy », « Joie » dans tout src/ : tendance en mots seulement, aucune alerte avant Aria stade 3.
[2026-09-24] | Règle « devoirs / emploi du temps réservés au collège » appliquée sans vérifier la réalité scolaire : Lucas (CM2) avait perdu ses devoirs et son emploi du temps. | Règles par niveau : maternelle = ni devoirs ni emploi du temps ; CP–CM2 = devoirs de primaire + journée type ; collège / lycée = par matière. En cas de doute sur une pratique scolaire, la noter [UNCLEAR] plutôt que retirer une fonction.
[2026-09-24] | Outil de découpe (cut) : le marqueur de fin était cherché à partir du marqueur de début inclus → avec un marqueur de fin préfixe du début (« <!-- »), rien n’était coupé et le bloc joie du mémo PDF est resté en B2-bis. | Chercher le marqueur de fin APRÈS le marqueur de début, et contrôler par grep que le texte visé a disparu avant d’annoncer une suppression.
[2026-09-24] | Consigne utilisateur après B2-ter : plus d’outil d’édition maison (scripts de découpe), et l’emploi du temps avait été codé alors que la consigne disait « sans coder ». | Édition native uniquement (Edit / Write), grep de contrôle après chaque suppression. Si une consigne dit « sans coder », DEMANDER avant de coder, même pour un petit changement.
[2026-09-24] | Voile de la bottom bar lié au défilement : invisible à scrollY = 0 alors qu’une liste longue passe déjà sous la barre → texte lisible à travers la pill Aria sur Android. | Un voile du bas ne dépend pas du défilement vers le haut : toujours visible. Sur Android, les vues sœurs sont ordonnées par élévation AVANT le zIndex : donner une élévation explicite (voile < barre).
[2026-09-25] | Suppression de lignes avec Edit dans des fichiers CRLF : un old_string COMMENÇANT par un saut de ligne a aussi mangé la fin de ligne suivante → lignes voisines collées (tsc OK, donc invisible sans diff). | Pour supprimer une ligne : old_string = ligne précédente entière + ligne à retirer, new_string = ligne précédente seule (jamais de saut de ligne en bord). Contrôle : `git diff --ignore-cr-at-eol --numstat` doit montrer 0 ajout.
[2026-09-25] | Le rapport B3-0 affirmait « VISION.md ne contient plus 3020 » : vrai en local, faux sur GitHub (origin/main 51 commits en retard, rien de poussé depuis). | Quand je cite l’état d’un fichier, préciser « en local » et signaler si origin/main est en retard (`git rev-list --count origin/main..main`).
[2026-09-25] | Emploi du temps : grand vide sous la bande des jours sur Android (Lucas ~500 px, Emma ~250 px). Cause : un ScrollView HORIZONTAL garde flexGrow: 1 par défaut ; dans une colonne flex:1 il se partage l’espace libre avec le ScrollView vertical voisin (le web ne le reproduit pas). | Tout ScrollView horizontal posé directement dans une colonne à hauteur fixe : style `flexGrow: 0, flexShrink: 0`. Un vide proportionnel à la place libre (moins de contenu = plus de vide) signale un partage de flexGrow, pas un centrage.
[2026-09-25] | Messages : « ~150 px » de vide sous la toolbar = 48 dp de marges empilées (padding + 24 dp de marge haute du 1er libellé de section, prévue pour séparer deux sections). | Une marge « entre sections » ne s’applique pas à la première section affichée. Convertir les pixels rapportés sur le Redmi en dp (densité 2,75) avant de chercher un bug de plateforme.
[2026-09-25] | INFRA-1 : la baseline (enregistrée « appliquée » à Londres sans jamais être exécutée) a échoué au premier vrai rejeu sur Paris : uuid_generate_v4() non préfixée, et le rôle temporaire de la CLI n'a pas `extensions` dans son search_path (la base locale Docker, si : les tests locaux ne l'ont pas vu). | Dans les migrations, toujours préfixer les fonctions d'extension (`extensions.uuid_generate_v4()`) et créer les extensions `WITH SCHEMA extensions`. Une migration jamais exécutée en distant n'est pas « testée » par un db reset local.
[2026-09-25] | Rappel de l'utilisateur (INFRA-1) : « quand tu poses une question, tu attends la réponse ». | Toute question posée (« J'applique ? », « Je continue ? ») = arrêt complet du tour ; aucune action liée tant que la réponse n'est pas arrivée, même si le correctif paraît évident.
[2026-09-26] | Menu du bouton année (Modal) ouvert 33 dp trop haut sur le Redmi, recouvrant le bouton : measureInWindow mesure depuis le bas de la barre d'état, alors que le Modal couvre tout l'écran en bord à bord (même sans statusBarTranslucent). Le web ne montrait rien. | Tout menu positionné par mesure dans un Modal : ajouter StatusBar.currentHeight sur Android, et MESURER sur le téléphone (position du bouton et du menu en dp via uiautomator) avant de valider.
